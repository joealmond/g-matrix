runConfig:
  minInstances: 0      # Scale to zero when not in use (saves money)
  maxInstances: 2      # Limit max instances for POC
  concurrency: 80      # Default concurrency
  cpu: 1               # Minimum CPU
  memoryMiB: 512       # Minimum memory
