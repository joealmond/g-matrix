rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all reads and writes by default. This is the most secure starting point.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
